localhost {
  log {
    format json
  }

  # Компрессия
  encode zstd
  # Самоподписной сертификат
  auto_https: off

  # Проксирование всех запросов в app:8080
  # app – имя сервиса в docker-compose.yml
  #  reverse_proxy /* app:8080
  root * /etc/caddy/index.html
}
